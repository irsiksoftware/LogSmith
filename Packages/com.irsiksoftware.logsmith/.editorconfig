# LogSmith package - disable ISU analyzers to prevent self-referential warnings
# LogSmith uses UnityEngine.Debug internally (not its own logging)

root = false

[*.cs]
generated_code = true
dotnet_analyzer_diagnostic.severity = none
